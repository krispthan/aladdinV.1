import React from 'react';

import './domain-model.scss';

/* eslint-disable-next-line */
export interface IDomainModelProps {}

export const DomainModel: React.FC<IDomainModelProps> = (props) => {
  return (
    <div>
      <h1>Welcome to domain-model!</h1>
    </div>
  );
};
