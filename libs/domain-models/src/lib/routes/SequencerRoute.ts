//This enum will hold all the sublevel component routes from Dashboard
export enum SequencerRoute {
  Dashboard = 'dashboard',
  Projects = 'projects',
  UploadData = 'upload-data',
  ManageSamples = 'manage-samples',
  Analysis = 'analysis',
  Report = 'report',
  Claim = 'claim',
}
